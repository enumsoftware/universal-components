import { UcTooltip } from './uc-tooltip';

describe('UcTooltip', () => {
  it('should create an instance', () => {
    const directive = new UcTooltip();
    expect(directive).toBeTruthy();
  });
});
