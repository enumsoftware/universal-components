import { defineShowcase } from '../workbench/core';

/** Docs only: the page is the sibling uc-defaults.docs.md. */
export default defineShowcase({
  id: 'foundations/default-variants',
  group: 'Foundations',
  title: 'Default Variants',
  layout: 'padded',
});
